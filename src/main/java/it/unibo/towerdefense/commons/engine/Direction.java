package it.unibo.towerdefense.commons.engine;

/**
 * 4 cardinal directions as 2D verosr.
 */
public enum Direction {
    /** East. */
    E,
    /** North. */
    N,
    /** West. */
    W,
    /** Sud. */
    S;
}
