package it.unibo.towerdefense.model;

/**
 * Interface that represents a generic manager.
 */
public interface Manager {

    /**
     * Binds the model manager to the manager.
     * @param mm the model manager
     */
    void bind(ModelManager mm);
}
